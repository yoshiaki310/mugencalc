machine:
  node:
    version:  5.5.0

dependencies:
  override:
    - npm install
    - bundle install
    - sudo pip install awscli

  cache_directories:
    - "node_modules"

deployment:
  production:
    branch: master
    commands:
      - ./deploy_staging.sh
